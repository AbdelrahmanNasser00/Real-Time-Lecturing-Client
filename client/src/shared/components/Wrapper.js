import { styled } from "@mui/system";

const Wrapper = styled("div")({
  width: "100%",
  height: "calc(100vh - 64px)",
  display: "flex",
  flexDirection: "row",
  position: "relative",
});

export default Wrapper;
