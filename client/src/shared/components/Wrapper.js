import { styled } from "@mui/system";

const Wrapper = styled("div")({
  width: "100%",
  height: "100vh",
  display: "flex",
  flexDirection: "row",
  position: "relative",
});

export default Wrapper;
